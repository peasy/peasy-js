var Configuration = (function() {

  "use strict";

  var Configuration = function(callbacks) {
  };

  Configuration.autoPromiseWrap = false;

  return Configuration;

})();

module.exports = Configuration;
